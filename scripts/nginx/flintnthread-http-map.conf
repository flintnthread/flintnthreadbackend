# DEPRECATED — no longer required after seller APIs moved to /api/seller/*.
# Safe to remove from /etc/nginx/conf.d/ if present.
#
# Previously routed shared /api/products|orders|colors|sizes|reviews by X-Seller-Id header.
