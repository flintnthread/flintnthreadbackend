# Admin API — add INSIDE nginx server { } for flintnthread.online
# MUST be placed BEFORE the general "location /api/" block (more specific wins).

location ^~ /api/admin/ {
    proxy_pass http://127.0.0.1:8082;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
}

# Seller admin review routes (optional — if not already proxied to 8083)
# location ^~ /api/admin/seller-profiles {
#     proxy_pass http://127.0.0.1:8083;
#     ...
# }
